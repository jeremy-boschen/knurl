name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  notify:
    name: Release workflow
    runs-on: ubuntu-latest
    steps:
      - name: Releases built locally
        run: |
          echo "Releases are built locally and uploaded manually."
          echo "See scripts/release.sh for the release process."
          exit 0
