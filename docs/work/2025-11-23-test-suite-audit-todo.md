TODO
- Audit existing test files.
- Classify each test file as unit, integration, rust, or e2e.
- Detect overlaps where the same behavior is tested in multiple layers.
- Detect violations where tests do work outside their proper layer.
- Recommend actions (delete, simplify, relocate tests, or add missing tests).
