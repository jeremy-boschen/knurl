pre-commit:
  parallel: true
  commands:
    format-all:
      run: yarn format
    lint-all:
      run: yarn lint

pre-push:
  parallel: false
  commands:
    test-unit:
      run: yarn test:unit

# commit-msg:
#   commands:
#     commitlint:
#       run: yarn commitlint --edit {1}
# TODO: Re-enable commitlint hook once Windows shebang parsing is fixed
